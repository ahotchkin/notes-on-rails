<h1>Add a Note</h1>

<%= render :partial => "layouts/errors", :locals => {:object => @note} %>

<%= form_for @note do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>
  <br>
  <%= f.label :body %>
  <%= f.text_area :body %>
  <br>
  <%= f.submit "Create Note" %>

<% end %>
